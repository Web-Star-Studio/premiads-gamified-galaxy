
import { memo } from 'react';
import CampaignFormComponent from "./campaign-form/CampaignForm";

/**
 * Campaign form component for creating and editing missions
 * Re-exported with memo for performance optimization
 */
export default memo(CampaignFormComponent);
