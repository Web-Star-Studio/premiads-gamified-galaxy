
import { CashbackCampaign } from '@/types/cashback';

// Empty mock data
export const MOCK_CASHBACK_CAMPAIGNS: CashbackCampaign[] = [];

// Empty mock data for advertiser logos and names
export const advertisers = [];

// Empty mock conditions for campaigns
export const conditions = [];

// Default mock cashback balance - set to 0
export const DEFAULT_CASHBACK_BALANCE = 0;
